<?xml version="1.0" encoding="UTF-8"?>
<items>
    {% for item in result %}
    <item id="{{ item.id }}">
        {% for property,value in item %}
        <{{ property }}>{{ value }}</{{ property }}>
        {% endfor %}
    </item>
    {% endfor %}
</items>