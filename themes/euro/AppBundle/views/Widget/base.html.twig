{% block widget_container %}
    <div class="portlet widget_container" id="{{ block_id }}">
        <div class="portlet-heading portlet-default">
            <h3 class="portlet-title text-dark">{{ widget.property('title') }}</h3>
            <div class="portlet-widgets">
                <a href="javascript:;" data-toggle="reload" id = "#{{ block_id }}_refresh"><i class="ion-refresh zTable-refresh"></i></a>
                <span class="divider"></span>
                <a data-toggle="collapse" data-parent="#accordion1" href="#{{ block_id }}_collapse"><i class="ion-minus-round"></i></a>
                <span class="divider"></span>
                <a href="{{ path('app.dashboard_widget_remove', {"widgetId": widget.getId()}) }}" class="widget-remove" data-toggle="remove"><i class="ion-close-round"></i></a>
            </div>
            <div class="clearfix"></div>
        </div>
        <div id="{{ block_id }}_collapse" class="panel-collapse collapse in">
            <div class="portlet-body">
                {{ widget_render_block(widget, 'widget') }}
            </div>
        </div>
    </div>
{% endblock widget_container %}

{% block widget_widget %}

{% endblock widget_widget %}

{% block widget_assetcss %}
    {% for child in widget.children() %}
        {{ widget_render_block(child, 'assetcss') }}
    {% endfor %}
    {{ asset_render('css') }}
{% endblock widget_assetcss %}

{% block widget_assetjs %}
    {% for child in widget.children() %}
        {{ widget_render_block(child, 'assetjs') }}
    {% endfor %}
{% endblock widget_assetjs %}

{% block widget_javascript %}
    {% for child in widget.children() %}
        {{ widget_render_block(child, 'javascript') }}
    {% endfor %}
{% endblock widget_javascript %}

{% block widget_stylesheet %}
    {% for child in widget.children() %}
        {{ widget_render_block(child, 'stylesheet') }}
    {% endfor %}
{% endblock widget_stylesheet %}